export interface Income {
  id: string;
  description: string;
  amount: number;
  category: string;
  user_id: string;
  created_at: string;
}

export interface Expenses {
  id: string;
  description: string;
  amount: number;
  category: string;
  user_id: string;
  created_at: string;
}
